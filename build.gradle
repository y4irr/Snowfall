allprojects {
    group = "vip.aridi.core"
    version = "2025.01.18"

}

tasks.register('Snowfall shared') {
    group = 'Snowfall'
    dependsOn ':shared:publishToMavenLocal'
}

tasks.register('Snowfall bukkit') {
    group = 'Snowfall'
    dependsOn ':bukkit:publishToMavenLocal'
}

tasks.register('Snowfall proxy') {
    group = 'Snowfall'
    dependsOn 'proxy:publishToMavenLocal'
}